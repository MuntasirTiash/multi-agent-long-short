# Core: the orchestration, features and evaluation are pure standard library
# (Python 3.9+). `requests` is the one hard dependency — price download from
# Yahoo's chart endpoint and the HTTP client for a local vLLM/Ollama server.
requests>=2.31

# ---- Optional: local, zero-cost LLM reasoning --------------------------------
# LLM_BACKEND=openai-compatible  -> nothing extra here; run vLLM/Ollama separately
#   vllm                          # GPU server for batched inference
# LLM_BACKEND=transformers       -> in-process HuggingFace inference
#   transformers>=4.44
#   torch>=2.0
# sentence-transformers           # optional real embeddings for agent memory

# ---- Optional: analysis / plotting scripts -----------------------------------
#   pandas                        # inspect_run.py, transcript analysis
#   matplotlib                    # plot_feature_citations.py
